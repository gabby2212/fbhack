<div class = "container-wrapper">

  <h1>Sent Payments</h1>
  <% if @sent_payments == nil%>
    <p>You haven't paid anyone yet </p>
  <% else %>
    <table class="center table table-hover" style="background-color: white">

    <thead><tr><th> From </th><th> Amount </th> </tr></thead>
    <% @sent_payments.each do |p| %>
          <tr>
          <td><%= p.receiver.email %></td>
          <td><%=number_to_currency p.amount%></td>

          </tr>
          
    <% end %>
    </table>
  <% end %>



  <h1>Received Payments</h1>
  <% if @received_payments == nil %>
    <p>No one has paid you yet</p>
  <% else %>
    <table class="center table table-hover" style="background-color: white">

    <thead><tr><th> To </th><th> Amount </th> </tr></thead>
      <% @received_payments.each do |p| %>

          <tr>
          <td><%= p.sender.email %></td>
          <td><%=number_to_currency p.amount%></td>

          </tr>
          
    <% end %>
    </table>
    <% end %>

<%= render 'layouts/footer' %>

</div>